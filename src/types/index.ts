export interface NavItem {
  label: string;
  href: string;
}

export interface ServiceCard {
  title: string;
  description: string;
  link: string;
}

export interface InsightCard {
  title: string;
  description: string;
  imageUrl: string;
  link: string;
}
